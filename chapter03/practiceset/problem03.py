# wap in python to detect double spacing in a string
name = "never let anyone define you"
print(name.find("  "))

#   if the output is -1 than there is no double spacing
#   and if any other number is in  output like 16 than double spacing  is there